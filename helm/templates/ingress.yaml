apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: regexec-ingress
  namespace: {{ .Values.namespace }}
  annotations: {}
spec:
  ingressClassName: {{ .Values.ingress.ingressClassName }}
  rules:
  - host: {{ .Values.ingress.host }}
    http:
      paths:
      - path: /socket.io
        pathType: Prefix
        backend:
          service:
            name: server-service
            port:
              number: 3001
      - path: /health
        pathType: Prefix
        backend:
          service:
            name: server-service
            port:
              number: 3001
      - path: /rooms
        pathType: Prefix
        backend:
          service:
            name: server-service
            port:
              number: 3001
      - path: /
        pathType: Prefix
        backend:
          service:
            name: client-service
            port:
              number: 80